# AI Chat App Frontend Environment Template
# Copy this file to .env and replace placeholder values

# Backend API Configuration
# For development: use localhost
# For production: use your deployed backend URL
VITE_API_URL=http://localhost:3000

# Stream Chat Configuration (Required)
# Use the same API key as backend (STREAM_API_KEY)
# DO NOT use the secret key here - frontend only needs the public key
VITE_STREAM_API_KEY=

# Observability Configuration (Optional)
# Frontend-specific Sentry DSN (different project from backend)
# 1. Create separate Sentry project for frontend
# 2. Copy DSN from project settings
VITE_SENTRY_DSN=

# Development Configuration
VITE_ENV=development